import 'package:flutter/material.dart';

class AppColors {
  // Fondos & Superficies
  static const Color whiteColor         = Color(0xFFFFFFFF);
  static const Color primaryColor       = Color(0xFF8F94FB);
  static const Color secondaryColor     = Color(0xFF647AB0);
  static const Color backgroundColor    = Color(0xFFF7F7F7);

  // Textos
  static const Color headerTextColor    = Color(0xFF466994);
  static const Color primaryTextColor   = whiteColor;
  static const Color secondaryTextColor = whiteColor;
  static const Color surfaceTextColor   = Color(0xFF000000);

  // App Bar
  static const Color appBarShadeColor   = Color(0xFFC8D8FF);

  // Estados
  static const Color successStatusColor = Color(0xFF4CAF50);
  static const Color warningStatusColor = Color(0xFFFFC107);
  static const Color errorStatusColor   = Color(0xFFFF5252);

  // Botones
  static const Color primaryButtonColor   = primaryColor;
  static const Color secondaryButtonColor = Color(0xFFE4EBFF);

  // Separadores
  static const Color dividerColor = Color(0xFFBDBDBD);
  static const Color borderColor  = Color(0xFFE0E0E0);
}