import 'package:flutter/material.dart';

class AppColors {
  // Fondos & Superficies
  static const Color backgroundColor    = Color(0xFF24293E);
  static const Color accentColor        = Color(0xFF8EBBFF);

  // Textos
  static const Color primaryTextColor   = Color(0xFFF4F5FC);
  static const Color secondaryTextColor = Color.fromRGBO(204, 204, 204, 1.0);

  // Estados
  static const Color successStatusColor = Color(0xFF4CAF50);
  static const Color warningStatusColor = Color(0xFFFFC107);
  static const Color errorStatusColor   = Color(0xFFFF5252);

  // Botones
  static const Color primaryButtonColor   = accentColor;
  static const Color secondaryButtonColor = Color(0xFF757575);

  // Separadores
  static const Color dividerColor = Color(0xFFBDBDBD);
  static const Color borderColor  = Color(0xFFE0E0E0);
}